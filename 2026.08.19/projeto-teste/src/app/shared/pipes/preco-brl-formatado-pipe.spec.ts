import { PrecoBrlFormatadoPipe } from './preco-brl-formatado-pipe';

describe('PrecoBrlFormatadoPipe', () => {
  it('create an instance', () => {
    const pipe = new PrecoBrlFormatadoPipe();
    expect(pipe).toBeTruthy();
  });
});
